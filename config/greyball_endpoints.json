{
  "_meta": {
    "purpose": "Declared public JSON endpoints a public page itself calls. Fetched as-is. Login/CAPTCHA/401/403/denied stops the adapter. Parameter mutation is refused.",
    "robots_tos_permit": false,
    "note": "Keep this list empty in production until a reviewer ticks robots/ToS permit per host. An empty list makes the collector abstain."
  },
  "robots_tos_permit": false,
  "collection_version": 1,
  "endpoints": []
}
