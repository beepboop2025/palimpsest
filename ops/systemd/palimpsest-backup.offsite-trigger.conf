[Unit]
# Install this drop-in only after the isolated offsite lane completes its first
# manual restore drill. It minimizes the mutable-local interval; the daily
# offsite timer remains the catch-up path after outages or missed starts.
OnSuccess=palimpsest-node-offsite-backup.service
